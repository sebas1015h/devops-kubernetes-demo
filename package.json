{
  "name": "devops-kubernetes-demo",
  "version": "1.0.0",
  "description": "Aplicación de demostración local preparada para un flujo CI/CD y Kubernetes.",
  "private": true,
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "node --watch src/app.js",
    "lint": "eslint .",
    "test": "node --test test/app.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "eslint": "^9.22.0",
    "globals": "^16.0.0",
    "supertest": "^7.0.0"
  }
}
